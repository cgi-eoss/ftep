group 'com.cgi.eoss.f-tep'
version '2.2.1'

apply plugin: 'base'

configurations { pkg }
artifacts {
    fileTree(dir: '.', includes: ['**/*.rpm']).collect {
        pkg it
    }
}

sonarqube {
    skipProject = true
}

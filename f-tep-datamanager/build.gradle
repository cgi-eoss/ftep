group 'com.cgi.eoss.f-tep'
version '0.3.1'

apply plugin: 'java'
apply plugin: 'jacoco'

dependencies {
    compileOnly deps.lombok

    compile project(':f-tep-common')
    compile project(':f-tep-orchestrator')

    compile deps.slf4j
}

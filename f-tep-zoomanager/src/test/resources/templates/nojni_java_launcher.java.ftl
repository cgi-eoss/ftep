import java.util.HashMap;

public class ${id} {

    public static int ${id}(HashMap conf, HashMap inputs, HashMap outputs) {
        System.out.println("Calling non-JNI service ${id}");
        return 3;
    }

}
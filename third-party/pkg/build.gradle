group 'com.cgi.eoss.f-tep'
version '2.4.4'

apply plugin: 'base'

configurations { pkg }
artifacts {
    fileTree(dir: '.', includes: ['**/*.rpm']).collect {
        pkg it
    }
}

sonarqube {
    skipProject = true
}
